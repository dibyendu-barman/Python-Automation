python -m venv .venv
.\.venv\Scripts\Activate.ps1
python --version
py --version
python -m pip --version
python -m pip install --upgrade pip
where.exe python

git config --global user.email "dibyendubarman96@gmail.com"
git config --global user.name "Dibyendu Barman"

git commit -m "Initial Python automation environment"
git remote add origin https://github.com/dibyendu-barman/Python-Automation.git
git config --global init.defaultBranch main
git config --global init.defaultBranch
git config --global core.autocrlf true 
git config --global color.ui auto

git init
git remote add origin https://github.com/dibyendu-barman/Python-Automation.git
git remote -v
git commit -m "Initial commit"
git branch -M main

git fetch origin
git log --oneline --graph --all --decorate -10

git pull origin main --allow-unrelated-histories
git status

git add .
git commit -m "Resolve merge conflict"
git push -u origin main